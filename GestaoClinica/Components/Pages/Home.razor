@page "/"
@using GestaoClinica.Data.Context
@using GestaoClinica.Entities
@using MudBlazor
@inject SQLServerDbContext DbContext
@using Microsoft.EntityFrameworkCore

<PageTitle>Lista de Pessoas</PageTitle>

<MudContainer>
    <MudPaper Elevation="3" Class="pa-4">
        <MudText Typo="Typo.h3" GutterBottom="true">Lista de Pessoas</MudText>

        @if (isLoading)
        {
            <MudProgressCircular Color="Color.Primary" Indeterminate="true" />
            <MudText>Carregando dados...</MudText>
        }
        else if (errorMessage != null)
        {
            <MudAlert Severity="Severity.Error">@errorMessage</MudAlert>
        }
        else if (!_pessoas.Any())
        {
            <MudAlert Severity="Severity.Info">Nenhuma pessoa cadastrada</MudAlert>
        }
        else
        {
            <MudTable Items="@_pessoas" Hover="true" Breakpoint="Breakpoint.Sm">
                <HeaderContent>
                    <MudTh>ID</MudTh>
                    <MudTh>Nome</MudTh>
                    <MudTh>Email</MudTh>
                    <MudTh>Telefone</MudTh>
                    <MudTh>CPF</MudTh>
                </HeaderContent>
                <RowTemplate>
                    <MudTd>@context.IdPessoa</MudTd>
                    <MudTd>@context.Nome</MudTd>
                    <MudTd>@context.Email</MudTd>
                    <MudTd>@context.Telefone</MudTd>
                    <MudTd>@context.CPF</MudTd>
                </RowTemplate>
            </MudTable>
            
            <MudText Class="mt-4">Total de pessoas: @_pessoas.Count</MudText>
        }
    </MudPaper>
</MudContainer>

@code {
    private List<Pessoa> _pessoas = new();
    private bool isLoading = true;
    private string errorMessage;

    protected override async Task OnInitializedAsync()
    {
        try
        {
            // Carrega os dados diretamente do DbContext
            _pessoas = await DbContext.pessoas.ToListAsync();
            
            if (_pessoas.Any())
            {
                Console.WriteLine($"Total de pessoas carregadas: {_pessoas.Count}");
            }
        }
        catch (Exception ex)
        {
            errorMessage = $"Erro ao carregar dados: {ex.Message}";
            Console.WriteLine($"ERRO: {ex}");
        }
        finally
        {
            isLoading = false;
        }
    }
}